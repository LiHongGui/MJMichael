git add -A && git commit -m "创建版本 4.1.2"

git tag -a 4.1.2 -m "tag release 4.1.2" #打tag

git push --tags #提交tag

git push origin master

pod spec lint

pod trunk push MJMichael.podspec --allow-warnings